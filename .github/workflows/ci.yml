name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: g++ -std=c++20 -Wall Board.cpp Game.cpp Player.cpp test_main.cpp -o tests

      - name: Run tests
        run: ./tests
